## Views

Esta pasta concentra os templates PHP usados pela interface.

Organizacao desta etapa:

- `layouts/` para partes reutilizaveis do layout
- `auth/` para login
- `dashboard/` para a tela inicial administrativa
- `clients/`, `installations/`, `users/` e `logs/` para modulos operacionais
- `errors/` para paginas de erro
