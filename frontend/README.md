## Frontend

Esta area concentra os templates e a organizacao visual da aplicacao.

Nesta base inicial:

- `views/layouts/` guarda layouts compartilhados
- `views/pages/` guarda paginas completas

Os assets publicos ficam em `public/assets/` para simplificar o primeiro bootstrap sem framework.
